apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustfs-pvc
  labels:
    app: rustfs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
